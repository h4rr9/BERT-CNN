{
  "exp":{
    "name": "conv_mnlim"
  },

  "data_loader":{
    "name": "MNLIM_data_loader.MNLIMDataLoader",
    "batch_size": 128
  },

  "model":{
    "name": "MNLIM_model.MNLIMModel",
    "optimizer": "adam"
  },

  "trainer":{
    "name": "MNLIM_trainer.MNLIMModelTrainer",
    "num_epochs": 10,
    "verbose_training": true
  },

  "callbacks":{
    "checkpoint_monitor": "val_acc",
    "checkpoint_mode": "max",
    "checkpoint_save_best_only": true,
   "checkpoint_save_weights_only": false,
    "checkpoint_verbose": true,
    "tensorboard_write_graph": true
  }
}
