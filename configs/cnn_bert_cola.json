{
  "exp":{
    "name": "conv_cola"
  },

  "data_loader":{
    "name": "CoLA_data_loader.CoLADataLoader",
    "batch_size": 128
  },

  "model":{
    "name": "CoLA_model.ConvCoLAModel",
    "optimizer": "adam"
  },

  "trainer":{
    "name": "CoLA_trainer.CoLAModelTrainer",
    "num_epochs": 10,
    "verbose_training": true
  },

  "callbacks":{
    "checkpoint_monitor": "val_matt_corr",
    "checkpoint_mode": "max",
    "checkpoint_save_best_only": true,
   "checkpoint_save_weights_only": true,
    "checkpoint_verbose": true,
    "tensorboard_write_graph": true
  }
}
